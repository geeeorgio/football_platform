{
  // The root config itself doesn't compile any source files
  "files": [],

  "references": [
    // Directs TS to look into specific sub-projects for type checking
    // { "path": "./packages/shared" },
    // { "path": "./apps/api" },
    // { "path": "./apps/web" },
    // { "path": "./apps/mobile" }
  ]
}
